import React from 'react';
import { MainContent } from '../../components';
import { Title } from '../../components/ui';

export const Trends = () => {
  return (
    <MainContent>
      <Title text={'Trends'} />
    </MainContent>
  );
};
