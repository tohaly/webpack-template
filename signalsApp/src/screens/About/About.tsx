import React from 'react';
import { MainContent } from '../../components';
import { Title } from '../../components/ui';

export const About = () => {
  return (
    <MainContent>
      <Title text={'About'} />
    </MainContent>
  );
};
